<div class="container mt-3">
  <h1>Restaurants</h1>
  <% @restaurants.each do |restaurant| %>
  <p><%= link_to restaurant.name, restaurant_path(restaurant) %> - <%=restaurant.category.capitalize%></p>
  <% end %>
  <%= link_to 'Create new restaurant', new_restaurant_path %>
</div>
